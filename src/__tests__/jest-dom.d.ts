// Ambient types — extend Jest's `expect` with @testing-library/jest-dom
// matchers for every .ts/.tsx file in src/. Pairs with jest.setup.ts which
// handles the runtime import.
import '@testing-library/jest-dom';
