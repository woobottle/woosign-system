# Dialog Component Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** @woosign/ui에 크로스 플랫폼(RN + Web) Controlled Dialog 컴포넌트를 추가한다.

**Architecture:** Toast 컴포넌트의 파일/스타일 규약을 따른다. Web은 `createPortal(document.body)`로 scrim+카드를 렌더하고 Esc/scrim 클릭으로 닫는다. Native는 RN 내장 `<Modal>`로 차단형 오버레이를 렌더한다. 표면/scrim/size 스타일은 `Dialog.styles.ts` 하나를 web/native가 공유하고, 서브컴포넌트(Header/Title/Description/Body/Footer)는 Card 패턴대로 standalone export + `Dialog` 객체 attach 둘 다 제공한다.

**Tech Stack:** React, React Native, TypeScript, react-dom `createPortal`, `@testing-library/react` + jest (jsdom `web` project, testMatch `*.web.test.tsx`).

---

## File Structure

```
src/components/Dialog/
  types.ts                # DialogSize, DialogProps(base/web/native), 서브컴포넌트 props, DialogContextValue
  DialogContext.ts        # aria id 전달용 React Context (web에서 사용)
  Dialog.styles.ts        # SIZE_MAX_WIDTH, SCRIM_COLOR, getDialogStyles()
  Dialog.web.tsx          # Dialog + DialogHeader/Title/Description/Body/Footer (web), attach
  Dialog.native.tsx       # 동일 구성 (RN <Modal>)
  Dialog.tsx              # web facade fallback → export from './Dialog.web'
  Dialog.web.test.tsx     # jsdom 동작 테스트
  Dialog.web.stories.tsx  # Storybook
  index.ts                # 타입 + 컴포넌트 re-export
```

Modify: `src/components/index.ts` — `export * from './Dialog'` 추가.

각 파일 책임이 분리되어 있다: 타입(types), context, 스타일, web 구현, native 구현, facade, 테스트, 스토리.

---

### Task 1: Scaffold types, context, styles

비동작(non-behavioral) 기반 파일. 테스트 없음 — 이후 태스크가 의존한다.

**Files:**
- Create: `src/components/Dialog/types.ts`
- Create: `src/components/Dialog/DialogContext.ts`
- Create: `src/components/Dialog/Dialog.styles.ts`

- [ ] **Step 1: types.ts 작성**

```ts
import type {ReactNode} from 'react';
import type {ViewStyle, TextStyle} from 'react-native';

/** Dialog 표면 폭 프리셋. */
export type DialogSize = 'sm' | 'md' | 'lg';

/** web/native 공통 컨테이너 props. */
export interface DialogBaseProps {
  /** 표시 여부 (controlled). */
  open: boolean;
  /** scrim 클릭 / Esc / Android back 시 호출. */
  onClose: () => void;
  /** 표면 폭 프리셋. 기본 'md'. */
  size?: DialogSize;
  /** scrim 클릭으로 닫을지. 기본 true. */
  closeOnScrimClick?: boolean;
  /** Esc(web) / Android back(native)로 닫을지. 기본 true. */
  closeOnEsc?: boolean;
  children?: ReactNode;
  testID?: string;
}

export interface DialogWebProps extends DialogBaseProps {
  className?: string;
  style?: React.CSSProperties;
}

export interface DialogNativeProps extends DialogBaseProps {
  style?: ViewStyle;
}

export type DialogProps = DialogBaseProps & {
  className?: string;
  style?: ViewStyle | React.CSSProperties;
};

/** 서브컴포넌트 공통 props. */
export interface DialogSectionProps {
  children?: ReactNode;
  style?: ViewStyle | React.CSSProperties;
  className?: string;
}

export type DialogHeaderProps = DialogSectionProps;
export type DialogTitleProps = DialogSectionProps;
export type DialogDescriptionProps = DialogSectionProps;
export type DialogBodyProps = DialogSectionProps;
export type DialogFooterProps = DialogSectionProps;

/** aria id 전달용 context 값 (web 한정 의미). */
export interface DialogContextValue {
  titleId: string;
  descriptionId: string;
}
```

- [ ] **Step 2: DialogContext.ts 작성**

```ts
import {createContext} from 'react';
import type {DialogContextValue} from './types';

/**
 * Dialog 표면이 생성한 aria id(titleId/descriptionId)를 서브컴포넌트
 * (DialogTitle/DialogDescription)로 전달한다. web에서만 의미가 있다.
 */
export const DialogContext = createContext<DialogContextValue | null>(null);
DialogContext.displayName = 'DialogContext';
```

- [ ] **Step 3: Dialog.styles.ts 작성**

```ts
import {
  colors,
  borderRadius,
  typography,
  spacing,
} from '../../core/theme/tokens';
import type {DialogSize} from './types';

/** size → 표면 maxWidth(px). */
export const SIZE_MAX_WIDTH: Record<DialogSize, number> = {
  sm: 360,
  md: 440,
  lg: 560,
};

/** scrim(배경 dimmer) 색. */
export const SCRIM_COLOR = 'rgba(0, 0, 0, 0.5)';

/**
 * web/native가 공유하는 표면·섹션 스타일. 숫자/문자 값만 담아
 * web(cssify)·native(StyleSheet) 양쪽에서 그대로 쓴다.
 */
export function getDialogStyles() {
  return {
    surface: {
      backgroundColor: colors.card,
      borderRadius: borderRadius.lg,
      overflow: 'hidden' as const,
      width: '100%' as const,
    },
    header: {
      paddingTop: spacing[6],
      paddingLeft: spacing[6],
      paddingRight: spacing[6],
      paddingBottom: spacing[2],
    },
    title: {
      fontFamily: typography.fontFamily.sans,
      fontSize: typography.fontSize.xl.size,
      lineHeight: typography.fontSize.xl.lineHeight,
      fontWeight: typography.fontWeight.semibold as '600',
      letterSpacing: typography.letterSpacing.tight,
      color: colors.textPrimary,
    },
    description: {
      fontFamily: typography.fontFamily.sans,
      fontSize: typography.fontSize.bodySm.size,
      lineHeight: typography.fontSize.bodySm.lineHeight,
      color: colors.textSecondary,
      letterSpacing: typography.letterSpacing.tight,
      marginTop: spacing[2],
    },
    body: {
      paddingTop: spacing[2],
      paddingLeft: spacing[6],
      paddingRight: spacing[6],
      paddingBottom: spacing[2],
    },
    footer: {
      paddingTop: spacing[4],
      paddingLeft: spacing[6],
      paddingRight: spacing[6],
      paddingBottom: spacing[6],
      flexDirection: 'row' as const,
      justifyContent: 'flex-end' as const,
      gap: spacing[3],
    },
  };
}
```

- [ ] **Step 4: 타입체크**

Run: `pnpm typecheck`
Expected: PASS (에러 없음). 아직 사용처가 없어도 컴파일된다.

- [ ] **Step 5: 커밋**

```bash
git add src/components/Dialog/types.ts src/components/Dialog/DialogContext.ts src/components/Dialog/Dialog.styles.ts
git commit -m "feat(Dialog): scaffold types, context, shared styles"
```

---

### Task 2: Web 컨테이너 (portal + scrim + Esc) — TDD

서브컴포넌트는 아직 없으므로, 이 태스크의 테스트는 `children`으로 평범한 `<div>`를 넣어 동작(열림/scrim/Esc/표면 전파 차단/role)을 검증한다.

**Files:**
- Create: `src/components/Dialog/Dialog.web.tsx`
- Create: `src/components/Dialog/Dialog.tsx` (facade — 이 태스크에서 같이 생성해야 테스트가 `'./Dialog'`를 import 가능)
- Test: `src/components/Dialog/Dialog.web.test.tsx`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/components/Dialog/Dialog.web.test.tsx`:

```tsx
/**
 * Web harness tests for Dialog. jsdom 환경, .web.tsx 구현을 사용한다.
 */
import {render, screen} from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import {Dialog} from './Dialog';

describe('Dialog (web)', () => {
  it('does not render content when closed', () => {
    render(
      <Dialog open={false} onClose={() => {}}>
        <div>본문</div>
      </Dialog>,
    );
    expect(screen.queryByText('본문')).not.toBeInTheDocument();
  });

  it('renders content when open with role=dialog and aria-modal', () => {
    render(
      <Dialog open onClose={() => {}}>
        <div>본문</div>
      </Dialog>,
    );
    expect(screen.getByText('본문')).toBeInTheDocument();
    const surface = screen.getByRole('dialog');
    expect(surface).toHaveAttribute('aria-modal', 'true');
  });

  it('calls onClose when scrim is clicked', async () => {
    const onClose = jest.fn();
    render(
      <Dialog open onClose={onClose} testID="dlg">
        <div>본문</div>
      </Dialog>,
    );
    await userEvent.click(screen.getByTestId('dlg-scrim'));
    expect(onClose).toHaveBeenCalledTimes(1);
  });

  it('does not call onClose on scrim click when closeOnScrimClick=false', async () => {
    const onClose = jest.fn();
    render(
      <Dialog open onClose={onClose} closeOnScrimClick={false} testID="dlg">
        <div>본문</div>
      </Dialog>,
    );
    await userEvent.click(screen.getByTestId('dlg-scrim'));
    expect(onClose).not.toHaveBeenCalled();
  });

  it('does not call onClose when the surface is clicked (no propagation)', async () => {
    const onClose = jest.fn();
    render(
      <Dialog open onClose={onClose}>
        <div>본문</div>
      </Dialog>,
    );
    await userEvent.click(screen.getByRole('dialog'));
    expect(onClose).not.toHaveBeenCalled();
  });

  it('calls onClose on Escape key', async () => {
    const onClose = jest.fn();
    render(
      <Dialog open onClose={onClose}>
        <div>본문</div>
      </Dialog>,
    );
    await userEvent.keyboard('{Escape}');
    expect(onClose).toHaveBeenCalledTimes(1);
  });

  it('does not call onClose on Escape when closeOnEsc=false', async () => {
    const onClose = jest.fn();
    render(
      <Dialog open onClose={onClose} closeOnEsc={false}>
        <div>본문</div>
      </Dialog>,
    );
    await userEvent.keyboard('{Escape}');
    expect(onClose).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `pnpm test -- Dialog.web.test`
Expected: FAIL — `Cannot find module './Dialog'` (아직 미작성).

- [ ] **Step 3: Dialog.web.tsx 작성 (컨테이너만; 서브컴포넌트는 Task 3)**

```tsx
/**
 * Dialog — Web implementation.
 *
 * createPortal(document.body)로 scrim+표면을 렌더해 부모 overflow/stacking
 * context를 탈출한다. scrim 클릭/Esc로 onClose를 호출하며, 표면 클릭은
 * stopPropagation으로 scrim까지 전파되지 않는다.
 */
import {useEffect, useId, useState} from 'react';
import {createPortal} from 'react-dom';
import type {DialogWebProps} from './types';
import {DialogContext} from './DialogContext';
import {getDialogStyles, SIZE_MAX_WIDTH, SCRIM_COLOR} from './Dialog.styles';
import {zIndex, shadowsCss} from '../../core/theme/tokens';

export function Dialog({
  open,
  onClose,
  size = 'md',
  closeOnScrimClick = true,
  closeOnEsc = true,
  children,
  className,
  style,
  testID,
}: DialogWebProps) {
  const titleId = useId();
  const descriptionId = useId();

  // SSR / hydration gate — createPortal requires document.body
  const [mounted, setMounted] = useState(false);
  useEffect(() => {
    setMounted(true);
  }, []);

  // Esc 리스너는 열려 있고 closeOnEsc일 때만 등록
  useEffect(() => {
    if (!open || !closeOnEsc) return;
    const onKeyDown = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose();
    };
    document.addEventListener('keydown', onKeyDown);
    return () => document.removeEventListener('keydown', onKeyDown);
  }, [open, closeOnEsc, onClose]);

  if (!open || !mounted || typeof document === 'undefined') return null;

  const s = getDialogStyles();

  return createPortal(
    <DialogContext.Provider value={{titleId, descriptionId}}>
      <div
        data-testid={testID ? `${testID}-scrim` : 'dialog-scrim'}
        onClick={closeOnScrimClick ? onClose : undefined}
        style={{
          position: 'fixed',
          inset: 0,
          backgroundColor: SCRIM_COLOR,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          padding: 16,
          zIndex: zIndex.modal,
          animation: 'wbDialogScrimIn 180ms ease-out',
        }}>
        <div
          role="dialog"
          aria-modal="true"
          aria-labelledby={titleId}
          aria-describedby={descriptionId}
          className={className}
          data-testid={testID}
          onClick={e => e.stopPropagation()}
          style={{
            ...s.surface,
            maxWidth: SIZE_MAX_WIDTH[size],
            boxShadow: shadowsCss.modal,
            animation: 'wbDialogSurfaceIn 180ms ease-out',
            ...style,
          }}>
          {children}
        </div>
        <style>
          {`@keyframes wbDialogScrimIn {
              from { opacity: 0; }
              to { opacity: 1; }
            }
            @keyframes wbDialogSurfaceIn {
              from { opacity: 0; transform: scale(0.96); }
              to { opacity: 1; transform: scale(1); }
            }`}
        </style>
      </div>
    </DialogContext.Provider>,
    document.body,
  );
}

Dialog.displayName = 'Dialog';
```

- [ ] **Step 4: Dialog.tsx facade 작성**

```tsx
// Web-side facade fallback: Metro reads src/ directly and resolves .native via platform extensions.
export {Dialog} from './Dialog.web';
```

- [ ] **Step 5: 테스트 실행 → 통과 확인**

Run: `pnpm test -- Dialog.web.test`
Expected: PASS (7 tests). `aria-labelledby`가 가리키는 노드는 Task 3에서 생기지만, 현재 테스트는 속성 존재까지 검증하지 않으므로 통과한다.

- [ ] **Step 6: 커밋**

```bash
git add src/components/Dialog/Dialog.web.tsx src/components/Dialog/Dialog.tsx src/components/Dialog/Dialog.web.test.tsx
git commit -m "feat(Dialog): web container with portal, scrim, and Esc"
```

---

### Task 3: Web 서브컴포넌트 (Header/Title/Description/Body/Footer) + attach — TDD

**Files:**
- Modify: `src/components/Dialog/Dialog.web.tsx` (서브컴포넌트 추가 + Dialog에 attach)
- Modify: `src/components/Dialog/Dialog.tsx` (서브컴포넌트 re-export)
- Test: `src/components/Dialog/Dialog.web.test.tsx` (테스트 추가)

- [ ] **Step 1: 실패하는 테스트 추가**

`Dialog.web.test.tsx`의 `describe` 블록 안 맨 끝에 추가:

```tsx
  it('renders subcomponents and wires aria-labelledby to the title', () => {
    render(
      <Dialog open onClose={() => {}}>
        <Dialog.Header>
          <Dialog.Title>제목</Dialog.Title>
          <Dialog.Description>설명</Dialog.Description>
        </Dialog.Header>
        <Dialog.Body>본문</Dialog.Body>
        <Dialog.Footer>
          <button>확인</button>
        </Dialog.Footer>
      </Dialog>,
    );
    const surface = screen.getByRole('dialog');
    const labelledBy = surface.getAttribute('aria-labelledby');
    const title = screen.getByText('제목');
    expect(title).toHaveAttribute('id', labelledBy);
    expect(screen.getByText('설명')).toBeInTheDocument();
    expect(screen.getByText('본문')).toBeInTheDocument();
    expect(screen.getByRole('button', {name: '확인'})).toBeInTheDocument();
  });
```

상단 import에 standalone 형태도 검증하기 위해 import 라인을 교체:

```tsx
import {Dialog} from './Dialog';
```
은 그대로 두고, 파일 하단에 standalone export 검증을 추가:

```tsx
  it('exposes subcomponents as standalone named exports', () => {
    // eslint-disable-next-line @typescript-eslint/no-var-requires
    const mod = require('./Dialog');
    expect(typeof mod.DialogHeader).toBe('function');
    expect(typeof mod.DialogTitle).toBe('function');
    expect(typeof mod.DialogDescription).toBe('function');
    expect(typeof mod.DialogBody).toBe('function');
    expect(typeof mod.DialogFooter).toBe('function');
  });
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `pnpm test -- Dialog.web.test`
Expected: FAIL — `Dialog.Header is not a function` / `mod.DialogHeader` undefined.

- [ ] **Step 3: Dialog.web.tsx에 서브컴포넌트 추가**

`Dialog.web.tsx` 상단 import에 유틸 추가:

```tsx
import {useContext} from 'react';
import {mergeStyles} from '../../core/variants';
import {cssifyWebStyles} from '../../core/utils/cssifyWebStyles';
import type {
  DialogHeaderProps,
  DialogTitleProps,
  DialogDescriptionProps,
  DialogBodyProps,
  DialogFooterProps,
} from './types';
```
(기존 `import {useEffect, useId, useState} from 'react';`는 `useContext`를 합쳐도 되고 위처럼 별도 줄로 둬도 된다.)

파일 하단(`Dialog.displayName` 다음)에 추가:

```tsx
export function DialogHeader({children, style, className}: DialogHeaderProps) {
  const s = getDialogStyles();
  const css = cssifyWebStyles(
    mergeStyles(s.header, style),
  ) as React.CSSProperties;
  return (
    <div className={className} style={css}>
      {children}
    </div>
  );
}
DialogHeader.displayName = 'DialogHeader';

export function DialogTitle({children, style, className}: DialogTitleProps) {
  const ctx = useContext(DialogContext);
  const s = getDialogStyles();
  const css = cssifyWebStyles(
    mergeStyles(s.title, {margin: 0}, style),
  ) as React.CSSProperties;
  return (
    <h2 id={ctx?.titleId} className={className} style={css}>
      {children}
    </h2>
  );
}
DialogTitle.displayName = 'DialogTitle';

export function DialogDescription({
  children,
  style,
  className,
}: DialogDescriptionProps) {
  const ctx = useContext(DialogContext);
  const s = getDialogStyles();
  const css = cssifyWebStyles(
    mergeStyles(s.description, {margin: 0}, style),
  ) as React.CSSProperties;
  return (
    <p id={ctx?.descriptionId} className={className} style={css}>
      {children}
    </p>
  );
}
DialogDescription.displayName = 'DialogDescription';

export function DialogBody({children, style, className}: DialogBodyProps) {
  const s = getDialogStyles();
  const css = cssifyWebStyles(mergeStyles(s.body, style)) as React.CSSProperties;
  return (
    <div className={className} style={css}>
      {children}
    </div>
  );
}
DialogBody.displayName = 'DialogBody';

export function DialogFooter({children, style, className}: DialogFooterProps) {
  const s = getDialogStyles();
  const css = cssifyWebStyles(
    mergeStyles(s.footer, style),
  ) as React.CSSProperties;
  return (
    <div className={className} style={css}>
      {children}
    </div>
  );
}
DialogFooter.displayName = 'DialogFooter';
```

이어서 컨테이너를 dot-notation까지 갖춘 단일 `Dialog`로 묶는다 (Task 4 native와 동일 패턴). 다음 3가지를 수행한다:

**(a)** Task 2에서 만든 `export function Dialog(...)` 선언을 `function DialogBase(...)`로 이름 변경하고, 그 함수 본문 바로 뒤에 있던 `Dialog.displayName = 'Dialog';` 라인은 삭제한다.

**(b)** 위 서브컴포넌트 5개 정의(`DialogHeader`~`DialogFooter`)를 둔다.

**(c)** 파일 맨 아래에 아래 블록을 추가한다 — 정적 프로퍼티 타입을 가진 인터페이스로 단언해 `Dialog.Header` 등 dot-notation을 타입 안전하게 노출한다:

```tsx
interface DialogComponent {
  (props: DialogWebProps): React.ReactElement | null;
  displayName?: string;
  Header: typeof DialogHeader;
  Title: typeof DialogTitle;
  Description: typeof DialogDescription;
  Body: typeof DialogBody;
  Footer: typeof DialogFooter;
}

const Dialog = DialogBase as unknown as DialogComponent;
Dialog.displayName = 'Dialog';
Dialog.Header = DialogHeader;
Dialog.Title = DialogTitle;
Dialog.Description = DialogDescription;
Dialog.Body = DialogBody;
Dialog.Footer = DialogFooter;

export {Dialog};
```

결과적으로 `Dialog`는 named export `DialogHeader`~`DialogFooter`와 dot-notation `Dialog.Header`~`Dialog.Footer`를 모두 제공한다.

- [ ] **Step 4: Dialog.tsx facade에 서브컴포넌트 re-export 추가**

```tsx
// Web-side facade fallback: Metro reads src/ directly and resolves .native via platform extensions.
export {
  Dialog,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogBody,
  DialogFooter,
} from './Dialog.web';
```

- [ ] **Step 5: 테스트 실행 → 통과 확인**

Run: `pnpm test -- Dialog.web.test`
Expected: PASS (9 tests).

- [ ] **Step 6: 타입체크**

Run: `pnpm typecheck`
Expected: PASS.

- [ ] **Step 7: 커밋**

```bash
git add src/components/Dialog/Dialog.web.tsx src/components/Dialog/Dialog.tsx src/components/Dialog/Dialog.web.test.tsx
git commit -m "feat(Dialog): web subcomponents with aria wiring + dot-notation API"
```

---

### Task 4: Native 구현 (RN Modal + 서브컴포넌트)

jest `web` 프로젝트는 `*.web.test.tsx`만 돌리므로 native 단위 테스트는 없다. 검증은 타입체크로 한다(호스트 앱이 RN E2E 소유 — Toast와 동일 정책).

**Files:**
- Create: `src/components/Dialog/Dialog.native.tsx`

- [ ] **Step 1: Dialog.native.tsx 작성**

```tsx
/**
 * Dialog — React Native implementation.
 *
 * RN 내장 <Modal>이 Android back 버튼(onRequestClose), 상태바, 전체화면
 * 오버레이를 처리한다. scrim Pressable 클릭으로 onClose, 표면 Pressable이
 * 터치 전파를 차단한다. closeOnEsc는 native에서 Android back 처리에 매핑된다.
 */
import {forwardRef} from 'react';
import {
  Modal,
  Pressable,
  View,
  Text,
  StyleSheet,
  type ViewStyle,
} from 'react-native';
import type {
  DialogNativeProps,
  DialogHeaderProps,
  DialogTitleProps,
  DialogDescriptionProps,
  DialogBodyProps,
  DialogFooterProps,
} from './types';
import {getDialogStyles, SIZE_MAX_WIDTH, SCRIM_COLOR} from './Dialog.styles';
import {shadows} from '../../core/theme/tokens';

function DialogBase({
  open,
  onClose,
  size = 'md',
  closeOnScrimClick = true,
  closeOnEsc = true,
  children,
  style,
  testID,
}: DialogNativeProps) {
  const s = getDialogStyles();
  return (
    <Modal
      visible={open}
      transparent
      animationType="fade"
      onRequestClose={closeOnEsc ? onClose : undefined}>
      <Pressable
        testID={testID ? `${testID}-scrim` : 'dialog-scrim'}
        onPress={closeOnScrimClick ? onClose : undefined}
        style={[styles.scrim, {backgroundColor: SCRIM_COLOR}]}>
        <Pressable
          testID={testID}
          onPress={() => {}}
          style={[
            s.surface as ViewStyle,
            shadows.modal,
            {maxWidth: SIZE_MAX_WIDTH[size]},
            styles.surface,
            style,
          ]}>
          {children}
        </Pressable>
      </Pressable>
    </Modal>
  );
}

export function DialogHeader({children, style}: DialogHeaderProps) {
  const s = getDialogStyles();
  return <View style={[s.header as ViewStyle, style as ViewStyle]}>{children}</View>;
}
DialogHeader.displayName = 'DialogHeader';

export function DialogTitle({children, style}: DialogTitleProps) {
  const s = getDialogStyles();
  return (
    <Text style={[s.title, style as object]}>
      {children}
    </Text>
  );
}
DialogTitle.displayName = 'DialogTitle';

export function DialogDescription({children, style}: DialogDescriptionProps) {
  const s = getDialogStyles();
  return (
    <Text style={[s.description, style as object]}>
      {children}
    </Text>
  );
}
DialogDescription.displayName = 'DialogDescription';

export function DialogBody({children, style}: DialogBodyProps) {
  const s = getDialogStyles();
  return <View style={[s.body as ViewStyle, style as ViewStyle]}>{children}</View>;
}
DialogBody.displayName = 'DialogBody';

export function DialogFooter({children, style}: DialogFooterProps) {
  const s = getDialogStyles();
  return <View style={[s.footer as ViewStyle, style as ViewStyle]}>{children}</View>;
}
DialogFooter.displayName = 'DialogFooter';

interface DialogComponent {
  (props: DialogNativeProps): React.ReactElement | null;
  displayName?: string;
  Header: typeof DialogHeader;
  Title: typeof DialogTitle;
  Description: typeof DialogDescription;
  Body: typeof DialogBody;
  Footer: typeof DialogFooter;
}

const Dialog = DialogBase as unknown as DialogComponent;
Dialog.displayName = 'Dialog';
Dialog.Header = DialogHeader;
Dialog.Title = DialogTitle;
Dialog.Description = DialogDescription;
Dialog.Body = DialogBody;
Dialog.Footer = DialogFooter;

export {Dialog};

const styles = StyleSheet.create({
  scrim: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    padding: 16,
  },
  surface: {
    width: '90%',
  },
});
```

> 참고: native `forwardRef` import는 사용하지 않으면 제거한다(위 코드는 사용하지 않음 — import 줄에서 `forwardRef` 삭제). lint 통과를 위해 미사용 import를 남기지 말 것.

- [ ] **Step 2: 타입체크**

Run: `pnpm typecheck`
Expected: PASS.

- [ ] **Step 3: lint**

Run: `pnpm lint`
Expected: PASS (미사용 import/변수 없음).

- [ ] **Step 4: 커밋**

```bash
git add src/components/Dialog/Dialog.native.tsx
git commit -m "feat(Dialog): react native implementation with RN Modal"
```

---

### Task 5: 배럴 export + Storybook + 최종 검증

**Files:**
- Create: `src/components/Dialog/index.ts`
- Create: `src/components/Dialog/Dialog.web.stories.tsx`
- Modify: `src/components/index.ts`

- [ ] **Step 1: index.ts 작성**

```ts
export type {
  DialogProps,
  DialogBaseProps,
  DialogWebProps,
  DialogNativeProps,
  DialogSize,
  DialogSectionProps,
  DialogHeaderProps,
  DialogTitleProps,
  DialogDescriptionProps,
  DialogBodyProps,
  DialogFooterProps,
  DialogContextValue,
} from './types';
export {
  Dialog,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogBody,
  DialogFooter,
} from './Dialog';
```

- [ ] **Step 2: src/components/index.ts에 Dialog export 추가**

`src/components/index.ts`에서 `export * from './Card';` 다음 줄에 추가:

```ts
export * from './Dialog';
```

- [ ] **Step 3: Storybook 스토리 작성**

`src/components/Dialog/Dialog.web.stories.tsx`:

```tsx
import {useState} from 'react';
import type {Meta, StoryObj} from '@storybook/react';
import {Dialog} from './Dialog';
import {Button} from '../Button';

const meta: Meta<typeof Dialog> = {
  title: 'Components/Dialog',
  component: Dialog,
  parameters: {layout: 'centered'},
};
export default meta;

type Story = StoryObj<typeof Dialog>;

function DialogDemo({size}: {size?: 'sm' | 'md' | 'lg'}) {
  const [open, setOpen] = useState(false);
  return (
    <>
      <Button onPress={() => setOpen(true)}>Dialog 열기</Button>
      <Dialog open={open} onClose={() => setOpen(false)} size={size}>
        <Dialog.Header>
          <Dialog.Title>주문을 취소할까요?</Dialog.Title>
          <Dialog.Description>이 작업은 되돌릴 수 없어요.</Dialog.Description>
        </Dialog.Header>
        <Dialog.Body>
          취소하면 결제도 함께 환불 처리됩니다. 계속하시겠어요?
        </Dialog.Body>
        <Dialog.Footer>
          <Button variant="secondary" onPress={() => setOpen(false)}>
            닫기
          </Button>
          <Button variant="destructive" onPress={() => setOpen(false)}>
            취소하기
          </Button>
        </Dialog.Footer>
      </Dialog>
    </>
  );
}

export const Default: Story = {render: () => <DialogDemo />};
export const Small: Story = {render: () => <DialogDemo size="sm" />};
export const Large: Story = {render: () => <DialogDemo size="lg" />};
```

- [ ] **Step 4: 전체 테스트 실행**

Run: `pnpm test`
Expected: PASS (tokens + web 프로젝트 모두 통과, Dialog.web.test 9 tests 포함).

- [ ] **Step 5: 타입체크 + lint**

Run: `pnpm typecheck && pnpm lint`
Expected: PASS.

- [ ] **Step 6: 커밋**

```bash
git add src/components/Dialog/index.ts src/components/Dialog/Dialog.web.stories.tsx src/components/index.ts
git commit -m "feat(Dialog): barrel exports, storybook, register in components index"
```

---

## Self-Review 메모

- **Spec coverage:** Controlled API(Task 2), size 매핑(styles+Task 2), scrim 닫기/closeOnScrimClick(Task 2), Esc/closeOnEsc + native back(Task 2/4), 서브컴포넌트(Task 3/4), 표면 전파 차단(Task 2/4), ARIA role/aria-modal/labelledby/describedby(Task 2/3), 토큰 활용(styles), 테스트 7+2(Task 2/3), index export(Task 5) — 모두 커버.
- **Naming 일관성:** `DialogBase`(내부) → `Dialog`(export), `SIZE_MAX_WIDTH`, `SCRIM_COLOR`, `getDialogStyles()`는 web/native/test 전반에서 동일하게 사용.
- **테스트 파일명:** jest `web` 프로젝트 testMatch가 `*.web.test.tsx`이므로 `Dialog.web.test.tsx` 사용(spec의 `Dialog.test.tsx`에서 정정).
- **알려진 트레이드오프:** Title 미사용 시 `aria-labelledby`가 존재하지 않는 id를 가리킨다(스크린리더는 무시). 차단형 focus trap/scroll lock은 범위 외(spec 합의).
```
